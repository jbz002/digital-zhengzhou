# Cloudflare Pages构建配置
name = "digital-zhengzhou"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# 构建配置
[build]
command = "npm run build"
cwd = "."
watch_dir = "src"

# 环境变量
[env.production.vars]
NODE_ENV = "production"